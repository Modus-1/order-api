namespace order_api
{
    /// <summary>
    /// An enum representing the status of an order.
    /// </summary>
    public enum OrderStatus
    {
        PLACED = 0,
        PROCESSING = 1,
        READY = 2
    }
}
